# PHP provider auto-runs composer install; we only add intl + frontend build.
[phases.setup]
nixPkgs = ["php82Extensions.intl", "nodejs_20"]

[phases.build]
cmds = [
  "npm ci",
  "npm run build"
]

[start]
cmd = "bash scripts/railway-start.sh"
